# Cerberus-Hybrid-NIDS
Cerberus-NIDS: Triple-Ensemble Intrusion Detection that leverages a Leader Class Confidence Decision Ensemble (LCCDE), it aggregates the strengths of XGBoost, LightGBM, and CatBoost to eliminate false positives, while using Unsupervised K-Means to detect zero-day anomalies.
